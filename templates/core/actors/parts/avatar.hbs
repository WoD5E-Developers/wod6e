<section
  class="sheet-part tab sheet-section {{ tabs.main.cssClass }}"
  data-group="primary"
  data-tab="main"
>
  <img
    class="actor-avatar"
    src="{{#if avatarURL}}{{ avatarURL }}{{else}}/icons/svg/mystery-man.svg{{/if}}"
    data-action="editImage"
    src="{{ img }}"
    data-edit="img"
  />
</section>
